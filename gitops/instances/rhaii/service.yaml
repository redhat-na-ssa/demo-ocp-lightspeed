apiVersion: v1
kind: Service
metadata:
  name: rhaii-vllm
  namespace: rhaii
  labels:
    app: rhaii-vllm
spec:
  selector:
    app: rhaii-vllm
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
