apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Adds spec.ols.quotaHandlersConfig (per-user and cluster-wide token quotas).
patches:
  - path: patch.yaml
    target:
      kind: OLSConfig
      name: cluster
