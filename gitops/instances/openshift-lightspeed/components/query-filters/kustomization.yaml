apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Adds spec.ols.queryFilters (redaction) to the OLSConfig.
patches:
  - path: patch.yaml
    target:
      kind: OLSConfig
      name: cluster
